# 组件索引

> 本文件记录 `prototype/components/` 下所有组件的索引及简要说明。
> Agent 在对任何组件做修改（新增组件、删除组件、重大变更）后，**必须**更新本文件。

## 组件列表

| 组件路径 | 说明 | 关键文件 |
|---------|------|---------|
| — | — | — |

> **路径格式**：`components/{hash}`，通过 CLI `node axhost-make/bin/axhost-make.js list` 获取名称/路径映射。

## 变更记录

| 日期 | 变更内容 |
|------|---------|
| — | 初始化 |

> 日期格式：`YYYY-MM-DD`。
